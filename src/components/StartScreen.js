import './StartScreen.css'

const StartScreen = () => {
  return (
    <div className="start" >
        <h1>Secret Word</h1>
        <p>Clique to start</p>
        <button>START</button>
    </div>
  )
}

export default StartScreen